module.exports = {
  presets: [
    ['@babel/preset-env', { targets: { node: 'current' } }], // Ensures compatibility with the Node.js runtime
    '@babel/preset-react', // Transforms JSX into JavaScript
  ],
};
